var fs = require('fs')

module.exports = {
    list: function (req, res) {
        // el usuario puede ver la lista de todos los productos favoritos
    },

    create: function (req, res) {
        // el usuario agrega un favorito a la lista
    },

    delete: function (req, res) {
        // el usuario elimino el producto de la lista
    }
}
